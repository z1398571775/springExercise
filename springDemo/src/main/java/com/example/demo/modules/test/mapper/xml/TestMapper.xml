<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.modules.test.mapper.TestMapper">
    <select id="getData" parameterType="java.lang.String" resultType="com.example.demo.modules.test.entity.Usertable">
        select * from usertable where id = #{id}
    </select>
</mapper>